/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTimeFormatePipePipe } from './DateTimeFormatePipe.pipe';

describe('Pipe: DateTimeFormatePipee', () => {
  it('create an instance', () => {
    let pipe = new DateTimeFormatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
